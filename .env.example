# Public - safe to expose in the browser bundle
VITE_TYPEFORM_FORM_ID=your-typeform-form-id

# Server-side only. Add the database connection string in Netlify's
# environment variables and locally when running the functions.
NETLIFY_DB_URL=postgres://your-user:your-password@your-host/your-database
