# Public - safe to expose in the browser bundle
VITE_TYPEFORM_FORM_ID=your-typeform-form-id

# Netlify Database connection is auto-provisioned and injected by Netlify —
# no manual env var needed. `netlify dev` also spins up a local Postgres
# instance automatically for local development.
